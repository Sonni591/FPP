package main;


public class Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		// Properties props = Configuration.getInstance().getProperties();
		// props.list(System.out);
		// Properties props2 = new Properties();
		// props2.setProperty("Werta", "valuea");
		// props2.setProperty("Wertb", "valueb");
		// props2.setProperty("Wertc", "valuec");
		// Configuration.getInstance().saveProperties(props2);
		// Configuration.getInstance().getProperties().list(System.out);

		// Properties props = new Properties();
		// props.setProperty("Username", "Tester");
		// props.setProperty("Passwort", "1243");
		//
		// props.list(System.out);
		// try {
		// props.store(new FileOutputStream("config.txt"), null);
		// } catch (FileNotFoundException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// } catch (IOException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }

		// Properties props = new Properties();
		// try {
		// props.load(new FileInputStream("config.txt"));
		// } catch (FileNotFoundException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// } catch (IOException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }
		// props.list(System.out);
		// props.setProperty("Username", "Ein Anderer");
		// props.list(System.out);
		//
		// try {
		// props.store(new FileOutputStream("config.txt"), null);
		// } catch (FileNotFoundException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// } catch (IOException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }

		// try {
		// String[][] outputValues = { { "outpout1", "1" },
		// { "outpout2", "2" }, { "outpout3", "3" } };
		// CSVPrint printer = new CSVPrinter(new FileOutputStream("test2.csv"));
		// printer.println(outputValues);
		// System.out.println("program end");
		// } catch (FileNotFoundException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }

		// try {
		// CSVParse parser = new CSVParser(new FileInputStream("test.csv"));
		// String[][] values = parser.getAllValues();
		// for (String[] strings : values) {
		// for (String string : strings) {
		// System.out.print(string + " ");
		// }
		// System.out.println();
		// }
		// } catch (FileNotFoundException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// } catch (IOException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }

	}

}
